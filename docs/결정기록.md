# 결정 기록

이 대시보드에서 무엇을 왜 그렇게 했는지 한 줄씩. Claude Code가 기능을 추가하거나 구조를 바꿀 때마다 아래에 한 줄 덧붙인다 — 날짜 · 무엇 · 어느 파일 · 비용(크레딧) · 이유.
한 달 뒤 돌아와도 "지난번에 이렇게 해두셨어요"라고 이어갈 수 있게 하는 기록이다.

## 템플릿 원칙 (바꾸지 않는 것)

| 날짜 | 결정 | 이유 |
|---|---|---|
| 2026-08-31 | 서버·DB·로그인 없음. 데이터는 `data/*.json` 4개 | 고치는 사람 한 명, 갱신은 하루·주 단위, 보기 전용 — 창고 서버가 필요한 조건이 하나도 없다. 계정 하나·고장 지점 하나가 줄어든다 |
| 2026-08-31 | 파일 주인 분리 — 사람 1개(settings) · 봇 3개(posts·discoveries·analysis) | 같은 파일을 사람과 봇이 같이 쓰면 git 충돌이 초보자 앞에서 터진다 |
| 2026-08-31 | 화면에 저장하는 버튼 없음. 전부 "Claude에게 말로" | 서버가 없어 반쪽 버튼(브라우저에만 저장)이 되기 때문. 반쪽이 제일 헷갈린다 |
| 2026-08-31 | 의존성 0, 빌드 없음, 라이브러리 없음(ECharts 제거) | npm 없이 ZIP 풀면 바로 돈다. 업데이트가 폴더 교체로 끝난다 |
| 2026-08-31 | 자동화는 GitHub Actions, 배포는 Vercel Git 연결 | 사용자 컴퓨터가 꺼져도 돈다. 시크릿 2개만 넣으면 끝 |
| 2026-08-31 | 수집 ScrapeCreators, 분석 Gemini. Composio(실측 지표) 제외 | 프로페셔널 전환·FB 페이지·OAuth 승인이 통째로 사라진다. 인스타 계정이 공개이기만 하면 된다 |
| 2026-08-31 | 발굴 문턱 = minViews 이상 + 소스 계정 평소(중앙값)의 2배. `DISCOVER_MEDIAN_MULT`로 조절 | 기획안은 3배였는데 실측(평소 8만 계정)에서 1페이지 0건. 본가는 1배. 소규모 소스가 많은 수강생은 0건이 제일 큰 실패 지점 |
| 2026-08-31 | 실패 카운터(`analyzeFails`·`coachFails`) 3회면 제외. 매일 스냅샷이 프로필 헤더 숫자도 갱신 | 삭제·비공개 영상이 매주 상위 슬롯을 점유해 크레딧을 태우는 구멍(본가에서 실제 발생). 헤더 팔로워가 주 1회만 바뀌면 매일 보는 맛이 없다 |
| 2026-08-31 | 더미 파일은 `demo:true`. 스크립트는 demo 를 '없음'으로 취급하고 첫 실행 때 더미 흔적(썸네일·settings 더미값·analysis·discoveries)을 지운다. 수집기가 `settings.json` 을 쓰는 유일한 예외 | 더미 발굴·스냅샷이 실데이터에 섞이고, 더미 릴스 수가 품질 게이트 기준이 돼 소규모 계정이 거부되던 구멍 |
| 2026-08-31 | 품질 게이트에 걸리면 경고만 남기고 기존 파일을 그대로 둔 뒤 정상 종료(0). 실패로 끊지 않는다 | 매일 도는 GitHub Actions 가 빨간 X 와 실패 메일을 보내면, 초보자는 "내가 뭘 잘못했나" 하고 손대다 멀쩡한 설정을 부순다. 데이터는 이미 지켜졌으니 남은 일은 알려주는 것뿐이다. 반대로 진짜 고장(계정 바뀜·키 오류·전 소스 실패)은 exit 1 로 끊어 초록불로 위장하지 않는다 |
| 2026-08-31 | SETUP 을 8단계로 쪼개고, 계정(ScrapeCreators·Gemini·GitHub·Vercel)을 만들 때마다 화면이 한 번씩 켜지게 배치 | 가입만 4번 몰아서 시키면 아무것도 안 보이는 상태로 30분을 버텨야 해서 중도 이탈이 난다. "가입 → 바로 뭔가 뜬다"를 반복해야 다음 가입을 견딘다. 수업에서 구간을 끊어 진행하기도 이 단위가 맞다 |
| 2026-08-31 | 더미 3세트(게시물·발굴·코칭)를 생성기 없이 사람이 직접 썼다 | 수업 첫 화면이 이 문장들이라, 자동 생성한 "샘플 텍스트 1"이 깔리면 대시보드가 무엇을 읽어주는 도구인지 전달되지 않는다. 문장 품질이 곧 설명이다. 본가의 더미 생성기는 이식하지 않았다 — 한 번 쓰고 끝인 데이터에 유지보수할 코드를 늘리지 않는다 |

## 사용자 기록

(Claude Code가 여기에 덧붙인다)

| 날짜 | 무엇 | 파일 | 비용 | 이유 |
|---|---|---|---|---|
| | | | | |

---

## 버그도감 대조 (2026-08-31)

세컨드 브레인 `버그도감/`(10개 갈래, B-001~B-214, 총 198건)을 이 저장소와 전수 대조한 결과. 코드는 이 대조 작업으로는 수정하지 않았다(표만 작성). 5개 에이전트가 01~10 갈래를 2개씩 나눠 맡아 코드를 직접 읽고 grep해 판정했다.

### 01·02 — 새어나가는 것 / 검증 안 한 값

Confirmed: no inline `onclick="..."` string concatenation remains, and every `title="${...}"` attribute goes through `U.esc()`. Both B-208 and B-213 fixes are verified in the live code.

Now compiling the final table.

| B번호 | 제목(축약) | 판정 | 근거/조치 |
|---|---|---|---|
| B-001 | 도어락코드 props 노출 | 해당없음 | React SSR·`use client` 자체가 없는 정적 fetch 앱. data/*.json은 애초에 전부 공개 파일이고 그 안에 도어락 코드 같은 "숨겨야 할 비밀값" 자체가 없음(공개 인스타 데이터·본인 채널 분석 텍스트뿐) |
| B-002 | 약한신원에 강한권한 | 해당없음 | 로그인·토큰·조회 경로가 없음. 인증 자체가 없는 무인증 정적 대시보드 |
| B-003 | ics 파일 출입코드 노출 | 해당없음 | 예약·캘린더(.ics) 기능 없음 |
| B-004 | 마스킹된 알림 방치 | 해당없음 | 관리자/코치 대상 실패 알림·문자 시스템 없음 |
| B-005 | 남의 hold 자리 해제 | 해당없음 | 예약 슬롯/소유권 개념 없음. GH Actions가 `concurrency: group data-write`로 순차 실행(동시성 이슈 자체가 설계상 없음) |
| B-103 | 무인증 코치정보 조회 | 해당없음 | 서버 API·로그인·임베드 자체가 없음(정적 파일 서빙뿐) |
| B-104 | admin_override 바디위조 | 해당없음 | 관리자 role, 쓰기 API 자체가 없음 |
| B-105 | key_prefix가 사실 전체키 | 해당없음 | 로그인 키·인증 로그 자체가 없음(무인증) |
| B-106 | RLS 빠진 테이블 반복 | 해당없음 | DB 자체가 없음(data/*.json 파일 저장) |
| B-107 | 코치/수강생 응답 혼입 | 해당없음 | AI 챗봇·대상별 응답 분리 개념 없음(단일 사용자용 코칭 텍스트) |
| B-108 | 위조 신원헤더 신뢰 | 해당없음 | 프록시·내부 신원 헤더 구조 없음 |
| B-109 | 키 꺼도 하루 더 로그인 | 해당없음 | 세션/로그인 자체가 없음 |
| B-206 | 세션 서명키 폴백 | 해당없음 | 세션 쿠키·관리자 로그인 자체가 없음 |
| B-208 | title·onclick XSS | 이미 적용됨 | `js/app.js` 전역에 인라인 `onclick="..."` 문자열조립 0건(grep 확인), `title="${...}"`는 전부 `U.esc()` 경유. `safeHref`/`safeImg`/`cssUrl`(app.js L37-47)로 href·img·CSS url() 별도 검증. `U.el()`(util.js L50-59)이 `addEventListener`로 값 분리 |
| B-213 | serve.js `.git` 노출 | 이미 적용됨 | `scripts/serve.js` L23: `p.split('/').some(s=>s.startsWith('.'))`로 전체 세그먼트 검사. L32: `HOST='127.0.0.1'` 고정 바인딩 |
| B-010 | 상한없는 숫자설정 | 해당없음 | 관리자 숫자 설정 폼 자체가 없음(설정은 Claude Code가 JSON 직접 편집). 유일한 `type=number`(발굴 임계값, min만 있음)도 상한 없이 커도 필터 결과가 비어질 뿐 크래시·나눗셈 없음 |
| B-011 | 금액 DB overflow 500 | 해당없음 | 금액 입력·DB integer 컬럼 없음 |
| B-012 | Number(null)=0 요일둔갑 | 해당없음 | 요일 인덱스 변환 없음. `U.fmt/num/pct`(util.js)는 `n==null` 등을 Number() 호출보다 먼저 명시적으로 가드해 이미 안전한 패턴 |
| B-013 | 저장 안 되는 항목 | 해당없음 | 설정 저장 API/allowlist 없음(읽기 전용 대시보드, 저장 버튼 자체 없음) |
| B-014 | 폼 문항 부분일치 오매칭 | 해당없음 | 외부 폼 문항 라벨 매칭 없음. ScrapeCreators API의 구조화 JSON 필드(`m.code`, `m.caption.text` 등)를 직접 읽음(`scripts/lib.js` normalizePost/normalizeGraphPost) |
| B-015 | 무한미래 예약점유 | 해당없음 | 예약 시스템 없음 |
| B-016 | 오타핸들 데이터 무검증 | 수정됨(2026-09-01) | `scripts/discover.js`(소스 계정)·`collect.js`(내 핸들) 둘 다 handle을 실제 신원 검증 없이 그대로 `scReels`/`scProfile`에 사용. 존재하지만 다른 사람 계정으로 오타 났을 때 조용히 그 계정 데이터가 섞임 — 등록 시 프로필 실조회 후 화면에 팔로워수 등 확인 절차 추가 필요 → `collect.js`는 프로필 조회 직후 `✅ 계정 확인 @핸들 · 표시명 · 팔로워 N명 · 게시물 N건 — 이 계정이 맞나요?` 를 찍고 비공개·게시물 0건이면 경고. `discover.js`는 **첫 등록 소스에만**(계정당 1크레딧, 두 번째 실행부터는 안 부름) 같은 확인 줄을 찍는다. SETUP.md 3단계 (1)·(2)가 이 출력을 그대로 읽어 확인받도록 개정 |
| B-017 | 캡션 절단 후 상업성 오분류 | 이미 적용됨 | `scripts/lib.js` L196-199: `commerceHint`를 `cutSafe` 절단 전 원문 캡션(`m.caption?.text`)으로 미리 계산해 저장(주석에 "300자 절단에 잘리기 전에 원문으로 판정"). `js/app.js`의 정규식은 폴백에서만 쓰임 |
| B-017 | 첫 후보 채택→true저장 | 해당없음 | 외부 폼 문항 후보 채택 로직 없음(구조화 API 필드 직접 사용) |
| B-018 | 필터조합 대형계정 전멸 | 이미 적용됨 | `scripts/discover.js` L23 `FIRST_DAYS` 기본값이 정확히 90일 — 해당 버그의 고친 값과 일치 |
| B-019 | 1000행 상한 소리없이 잘림 | 해당없음 | Supabase/PostgREST 없음(JSON 파일). 오히려 `discover.js`에 명시적 `MAX_ITEMS=300` 상한 + 로그 출력 존재 |
| B-110 | CSV 줄바꿈 파싱깨짐 | 해당없음 | CSV 업로드 기능 없음 |
| B-111 | 전화번호 오타 매칭실패 | 해당없음 | 전화번호 필드·매칭 로직 없음 |
| B-112 | pgvector 2000차원 상한 | 해당없음 | 벡터 DB/임베딩 인덱스 없음 |
| B-113 | localStorage 파싱깨짐 | 해당없음 | `discThr` 하나만 저장하며 `JSON.parse` 자체를 안 씀(`Number()`만 사용). `app.js` L851-852에서 파싱 실패시 자연스럽게 기본값 폴백 — 이미 안전 |
| B-114 | .or() 콤마 인젝션 | 해당없음 | PostgREST/DB 쿼리 없음 |
| B-115 | 관리자 자기잠금 | 해당없음 | 관리자 role·키 재발급 기능 없음 |
| B-188 | 숫자칸 휠스크롤 오염 | 수정됨(2026-09-01) | `js/app.js` L863 발굴 임계값 input이 `type="number"`이며 코드 전체에 `wheel` 가드가 전무(grep 0건). 금액 칸은 아니지만 동일 클래스 — 스크롤 중 값이 바뀐 채 blur 시 그대로 적용됨. `type="text" inputMode="numeric"`로 바꾸거나 wheel 가드 추가 권장 → `type="text" inputmode="numeric" pattern="[0-9]*"` 로 교체. 휠·화살표 조작 자체가 사라져 가드가 필요 없어졌고(브라우저 실측: 휠 굴려도 값 불변), `index.html`의 스핀버튼 숨김 CSS도 함께 제거 |
| B-202 | 설정 교차검증 없음 | 해당없음 | 설정 저장 API·교차검증 로직 자체가 없음(폼 없음) |
| B-203 | 변수명이 단위를 속임 | 해당없음 | 슬롯/시간 단위 변환 개념 없음 |

**조치 필요 건수: 2건 → 2건 모두 수정됨(2026-09-01)**

- B-016 — 소스 계정·내 핸들 등록 시 실제 인스타 프로필 존재·동일인 여부를 검증하지 않음 (`scripts/discover.js`, `scripts/collect.js`). 오타 핸들이 실존하는 다른 계정을 가리키면 조용히 그 데이터가 섞여 들어감.
- B-188 — `js/app.js`의 발굴 임계값 `type="number"` 입력창에 마우스 휠 가드가 없음. 스크롤 중 값이 조용히 바뀐 채 blur 시 그대로 적용될 수 있음.

나머지 32건 중 30건은 이 저장소에 해당 상황(로그인·세션·DB·결제·CSV업로드·예약·전화번호매칭·벡터인덱스 등) 자체가 없어 "해당없음"이며, B-208·B-213·B-017(캡션절단)·B-018 4건은 이미 코드에 정확히 반영돼 있음을 직접 확인했습니다(특히 B-018은 `FIRST_DAYS` 기본값 90일이 버그도감의 고친 값과 정확히 일치).
### 03·04 — 시간과 날짜 / 문자와 외부연동

## 대조 결과표

| B번호 | 제목(축약) | 판정 | 근거/조치 |
|---|---|---|---|
| B-020 | 문자 날짜 undefined | 해당없음 | 문자발송·손님용 폼/웹훅 자체가 없음(정적 대시보드, 로그인·발송 기능 無) |
| B-021 | 없는 날짜 조용히 넘어감 | 해당없음 | 사용자가 날짜 문자열을 입력하는 폼이 없음. 날짜는 전부 인스타 API의 `taken_at`/`timestamp` ISO값을 그대로 파싱 |
| B-022 | 전화번호 정규화 안 함 | 해당없음 | 전화번호를 다루는 기능 자체가 없음 |
| B-116 | 관리자 통계 '오늘'이 UTC 9시간 밀림 | 수정됨(2026-09-01) | `js/app.js:270` `pillarPeriodList()`의 `new Date().toISOString().slice(0,7)`이 브라우저 로컬(KST) 시각을 UTC로 강제 변환 — 매월 1일 00:00~09:00 KST에 "이번 달" 라벨이 지난달로 잘못 표시됨. 반면 `scripts/lib.js:427`의 `todayKST()`는 Node/GitHub Actions(UTC 서버)용으로 이미 올바르게 보정돼 있음(같은 교훈이 브라우저 쪽 코드까지는 안 퍼짐, B-116 원 사례와 동일 패턴). `getFullYear()/getMonth()` 기반 로컬 계산으로 바꿔야 함 → `monthKeyLocal()`(`getFullYear`/`getMonth`)을 새로 만들어 교체. 이 함수는 `js/app.js`의 `PURE-BLOCK` 안에 두어 `scripts/test/app-pure.test.js`가 "로컬 9월 1일 02시 → 2026-09"를 회귀 검증한다 |
| B-117 | 만료 상태가 연기 문 잠금 | 해당없음 | 예약·세션·상태 크론이 없는 정적 분석 대시보드 |
| B-205 | 입금기한 임박 항등식 | 해당없음 | 입금·기한·리마인더 개념 자체가 없음 |
| B-030 | HTTP 200을 성공으로 오판 | 이미 적용됨 | `scripts/lib.js:118` `sc()` — `if (res.ok && json.success !== false) return json;`로 본문의 `success` 필드까지 확인. 상태코드만으로 성공 판정하지 않음 |
| B-031 | 추적 번호 버림 | 해당없음 | 발송·도달 확인 개념이 없는 스크레이핑 API(요청=즉시 응답, 재확인 불필요) |
| B-032 | 수신거부 vs 일반실패 미구분 | 해당없음 | 수신거부 개념 없음. 다만 동일 원리(영구실패 vs 재시도 가능 실패 구분)는 `sc()`(4xx 즉시 throw·재시도 안 함, 5xx만 지수백오프)와 `discover.js:33` `isKeyOrCreditError()`(키/크레딧 오류는 남은 소스 다 태우지 않고 즉시 exit 1)에 이미 반영돼 있음 |
| B-033 | 번호 단위 완전단절 판정 불가 | 해당없음 | 수신자 개념이 없어 "이 사람에게 안 닿는다" 판정 자체가 무의미 |
| B-034 | 차단 판정 기간집계 오류 | 해당없음 | 동일한 건강판정/차단판정 로직 없음 |
| B-035 | 발신번호 조용히 죽음 | 해당없음 | 발신번호 등록·만료 개념이 없음. 키 실효 시에는 `sc()`가 4xx로 즉시 throw → `main().catch()`에서 `exit 1`(GitHub Actions 빨간불)로 드러나 "아무 신호 없음" 상황 자체가 구조적으로 발생하지 않음 |
| B-036 | 오류 응답 두 갈래 중 한쪽만 읽음 | 이미 적용됨 | `sc()`의 4xx 분기(`scripts/lib.js:119-121`)는 특정 필드명 하나만 읽지 않고 `JSON.stringify(json)` 전체를 덤프. 성공/실패 판정도 특정 오류 필드가 아니라 `res.ok`+`json.success`로만 하므로 응답 형태가 달라져도 판정이 안 흔들림 |
| B-037 | 문구 자리 안 채워짐 | 해당없음 | 사용자 편집 가능한 발송 문구 템플릿이 없음(Gemini 프롬프트는 코드 상수) |
| B-038 | 웹훅 시크릿 trim 누락 | 해당없음 | 웹훅·인증·시크릿 비교 로직이 없음(로그인 자체 없음) |
| B-039 | 경고 붙이다 추적정보 소실 | 해당없음 | `item.status=...`, `item.analyzeFails=...` 등 전부 필드 단위 할당이며, 기존 값을 담은 객체를 통째로 새로 만들어 덮어쓰는 패턴은 grep으로 확인한 결과 코드베이스에 없음 |
| B-118 | 두 시스템 공유 비밀번호 어긋남 | 해당없음 | 서버·인증·비밀번호 공유 구조가 없는 단일 정적 저장소 |
| B-119 | 운영환경 봇필터가 정상요청 403 | 해당없음 | 자체 프록시/Worker/CDN 계층이 없음 — GitHub Actions에서 ScrapeCreators·Gemini API로 직접 fetch |
| B-120 | 한글 파일명 NFD로 저장/헤더 실패 | 해당없음 | 썸네일 파일명은 인스타 shortcode(`r_${shortcode}.jpg`)·handle(`_avatar_${handle}.jpg`) 등 ASCII 식별자만 사용, 사용자가 짓는 한글 파일명을 저장 키로 쓰는 경로가 없음 |
| B-121 | 노션 API 100개 페이지 한도 가정 | 해당없음 | 노션 연동 없음. 다만 동일 원리(한 번에 다 온다고 가정하지 않기)는 `scReels`/`scPosts`(`scripts/lib.js:238-293`)가 커서(`max_id`/`cursor`) 기반 페이지네이션으로 이미 올바르게 구현 |
| B-122 | 지우고 다시쓰기 중 죽으면 기존 것도 사라짐 | 수정됨(2026-09-01) | `discover.js:152-162` 보관 상한 정리 로직이 `fs.unlinkSync`로 오래된 썸네일 **파일을 먼저 지우고** `db.items = keep` 후 맨 마지막에 `writeJson('discoveries.json', db)`(169번째 줄)로 **JSON은 나중에** 쓴다. 파일 삭제와 JSON 쓰기 사이에 프로세스가 죽으면, 기존 JSON(아직 갱신 전)은 여전히 그 항목들을 참조하는데 썸네일 파일은 이미 사라져 깨진 이미지가 남는다. 순서를 "JSON 먼저 쓰기 → 그다음 미참조 파일 삭제"로 바꿔야 최악의 경우가 "고아 파일 잔존(무해)"으로 바뀜 → 지울 목록을 `orphanThumbs` 배열에 모아만 두고, `writeJson('discoveries.json')`(lib.js의 임시파일→rename 원자적 쓰기) 뒤에 unlink 하도록 순서 교체. 이제 중간에 죽어도 남는 건 아무도 안 가리키는 파일뿐이다 |
| B-123 | SDK가 환경변수 자동 주입 | 해당없음 | Gemini/ScrapeCreators 모두 SDK 없이 `fetch`로 하드코딩된 URL 상수(`G`, `API`)를 직접 호출 — SDK의 암묵적 env 자동 주입 메커니즘 자체가 없음 |
| B-124 | SDK 편의 getter가 빈 문자열 반환 가능 | 수정됨(2026-09-01) | `geminiText()`(`scripts/lib.js:400-423`)는 `if (!txt.trim()) throw new Error('빈 응답')`로 빈 응답을 재시도 트리거로 막아둔 반면, `geminiAnalyze()`(361-396행)의 `return extractJson(gj.candidates?.[0]?.content?.parts?.[0]?.text || '')`는 빈 텍스트 검증이 없어 `finishReason`이 MAX_TOKENS/SAFETY이거나 파트가 비어도 `{raw:''}`를 조용히 반환하고 `analyzed`로 넘어갈 수 있음(`analyze.js`/`coach.js` 양쪽 다 이 경로 사용). `geminiText`와 동일한 빈 응답/`finishReason` 검증을 `geminiAnalyze`에도 추가해야 함 → 두 함수가 공유하는 `geminiCall()`을 새로 만들어 빈 응답·HTTP 오류·`finishReason` 검사를 한 곳으로 모았다. `geminiAnalyze`는 이제 빈 응답이면 예외를 던져 `analyzeFails` 카운터로 넘어가고, JSON이 아니면 1회 재시도한다 |
| B-125 | 경보 카드가 DB 제약에 막힘 | 해당없음 | DB·타 시스템 인박스 쓰기 자체가 없음 |
| B-126 | 레퍼런스 썸네일이 외부 임시 URL 참조 | 수정됨(2026-09-01) | `downloadThumb()`(`scripts/lib.js:311-327`)가 모든 신규 수집 경로(`discover.js:129`, `collect.js:102/109`)에서 즉시 로컬 저장하므로 "일회성 이관만 하고 신규 유입은 방치"라는 B-126의 핵심 문제는 이미 해결됨. 다만 B-126이 지적한 잔여 결함(Content-Type 미검증)은 여기도 그대로 있음 — `downloadThumb`은 `res.ok`만 확인하고 응답 바이트 수·`Content-Type`을 검사하지 않아, 인스타 CDN이 200으로 로그인월/에러 HTML을 주면 그대로 `.jpg`로 저장될 수 있음. `Content-Type: image/*` 확인(+최소 바이트 하한) 추가 필요 → `downloadThumb`에 `Content-Type` 이 `image/` 로 시작하는지 + 1024바이트 하한 검사를 넣고, 걸리면 **파일을 아예 만들지 않고** null 을 돌려준다. 호출부는 `thumb: null` 로 두고 화면은 🎬 로 폴백한다(만료될 외부 URL 을 대신 적어두지 않는다). `discover.js`는 실패 건수를 로그로 알린다 |
| B-190 | 플레이스홀더 오기입(시간↔기한) | 해당없음 | 사용자 발송 문구 템플릿이 없음 |
| B-192 | 취소 사유 문구 불일치 | 해당없음 | 동일 |
| B-199 | 리마인드 중복방지 키에 날짜 없음 | 해당없음 | 리마인드/예약 변경 흐름이 없음 |
| B-204 | 실패사유를 본문에 붙여 재발송 노출 | 해당없음 | 재발송·문자 기록 개념이 없음 |
| B-212 | 수집 래퍼가 예외 삼켜 401에도 초록불 | 이미 적용됨 | 이 저장소에서 실제로 고친 원 사례. `scReels()`(`scripts/lib.js:242-257`) 첫 페이지 실패는 throw, 뒷페이지 실패만 삼킴. `discover.js:70-81`의 `isKeyOrCreditError()`가 키/크레딧 오류를 감지하면 `exit 1`. `collect.js`의 `scReels`/`scProfile` 호출은 try/catch 없이 호출돼 실패 시 `main().catch()`(170행)로 전파되어 역시 `exit 1`. `snapshot.js`도 동일 구조로 확인 |

---

**조치 필요 건수: 4건 → 4건 모두 수정됨(2026-09-01)**

- **B-116** — `js/app.js:270`의 `new Date().toISOString().slice(0,7)`이 브라우저 로컬시각을 UTC로 강제 변환해, 매월 1일 00:00~09:00 KST에 "이번 달" 라벨이 지난달로 잘못 표시됨. `getFullYear()/getMonth()` 기반 로컬 계산으로 교체 필요.
- **B-122** — `discover.js` 보관 상한 정리 시 썸네일 파일을 먼저 삭제하고 JSON은 맨 마지막에 쓰는 순서라, 중간에 프로세스가 죽으면 JSON엔 남아있는데 파일은 사라진 상태가 될 수 있음. "JSON 먼저 쓰고 그 뒤에 미참조 파일 삭제" 순서로 변경 필요.
- **B-124** — `geminiAnalyze()`에 `geminiText()`와 같은 빈 응답/`finishReason` 검증이 빠져 있어, 영상 시각분석이 조용히 `{raw:''}`로 저장되고 `analyzed` 처리될 수 있음. 동일한 빈 응답 가드 추가 필요.
- **B-126** — `downloadThumb()`이 응답의 `Content-Type`/최소 바이트 검증 없이 그대로 저장해, 인스타 CDN이 로그인월 HTML(200 응답)을 주면 `.jpg`로 잘못 저장될 수 있음. `image/*` Content-Type 확인 추가 필요.

(대조 시점에는 코드를 고치지 않았고, 2026-09-01 에 위 4건을 실제로 수정했습니다.)
### 05·06 — 동시에 일어나는 일 / 조용한 실패

| B번호 | 제목(축약) | 판정 | 근거/조치 |
|---|---|---|---|
| B-040 | 확인후저장 이중예약 | 해당없음 | 예약/슬롯 시스템 자체 없음(정적 읽기전용) |
| B-041 | 버튼연타 이중처리 | 해당없음 | POST/상태변경 버튼 없음(읽기전용, util.js 주석) |
| B-042 | 만료자리 되살리기 충돌 | 해당없음 | 예약/슬롯 시스템 없음 |
| B-043 | 홀드무제한 점유 | 해당없음 | 익명 방문자 쓰기 자원 없음 |
| B-044① | 오래된PC수집이 서버덮음 | 이미 적용됨 | 데이터가 git 자체. git push는 비-FF 거부. SETUP.md 막힐때표 "봇커밋과 겹침→pull --rebase→충돌시 원격(theirs) 채택 후 로컬 재실행" 절차로 stale-local 우선 방지 |
| B-045 | 웹편집 config가 자동덮임 | 이미 적용됨 | brief.js는 매번 settings.json 재읽어 brief 필드만 교체(핸들·기둥·소스·overrides 보존), weekly.yml은 brief.js를 아예 안 부름 |
| B-046 | 병렬요청 카운터 lost update | 해당없음 | 로그인/실패카운터 없음 |
| B-044② | 쿠키버리면 점거제한 무력화 | 해당없음 | 익명 쓰기 API 없음 |
| B-127 | 미매칭큐 중복(maybeSingle) | 해당없음 | DB/큐 없음. discover.js는 Set(shortcode) 기반 중복판정이라 구조적으로 오판 불가 |
| B-128 | 컬렉션연타 저장충돌 | 해당없음 | 저장 버튼/PUT 엔드포인트 없음 |
| B-129 | 링크형식달라 중복적재 | 이미 적용됨 | collect.js·discover.js 모두 lib.js의 공용 normalizePost/normalizeGraphPost가 뽑은 shortcode를 유일 식별자로 사용(URL 문자열 비교 아님), 두 경로가 함수 공유 |
| B-130 | 컨텐츠중복유입 방지없음 | 해당없음 | 지식베이스형 다중경로 누적 구조 아님. 각 스크립트 자체 Set(shortcode)로 매회 중복 제거, posts.json/discoveries.json은 별개 파일이라 교차중복 없음 |
| B-050 | 로그인지연 서버리스 무용 | 해당없음 | 로그인·서버리스 함수 자체 없음(정적사이트+GH Actions 배치) |
| B-051 | 잠금칸 공유 | 해당없음 | 로그인 없음 |
| B-052 | 정시발송 부를주체 없음 | 이미 적용됨 | daily(매일)·discover(3일)·weekly(일요일) GH Actions cron이 정확히 이 역할 수행 |
| B-053 | 실패시 흐름정책 미정 | 이미 적용됨 | collect.js/discover.js 전반에 "왜 계속/왜 중단"이 주석으로 명시(피드실패는 건너뜀, 첫페이지실패는 안삼킴, 키/크레딧오류는 즉시중단) |
| B-054 | 백업이 0행을 정상기록 | 이미 적용됨 | collect.js qualityGate(신규<기존÷2면 파일보존), discover.js 전소스실패시 exit(1), analyze/coach.js 전량실패시 exit(1) |
| B-055 | 봇커밋 무료Vercel 미배포 | 수정됨(2026-09-01) | daily/discover/weekly.yml이 전부 `user.name "dashboard-bot"/user.email "bot@users.noreply.github.com"`로 커밋. SETUP.md 6단계가 무료 Vercel(Hobby)+GitHub 연동 안내, 막힐때표에 "Blocked — commit email" 항목이 있어 팀도 현상은 인지하나 근본원인(봇 커밋자=계정소유자 아님)이 안 고쳐져 매 자동수집마다 재발 가능. 이 저장소는 DB가 아니라 정적 JSON 자체가 데이터라 content-agent 사례보다 더 심각(사이트가 영구히 구데이터 정지 가능). 조치: 봇 커밋 이메일을 계정소유자 GitHub 이메일로 맞추거나 Vercel 설정 조정을 SETUP 7단계에 선제 안내로 승격 → 세 워크플로의 커밋 작성자를 `github.repository_owner` 기반으로 바꿨다. 실행 때마다 `gh api users/$OWNER` 로 `<ID>+<로그인>@users.noreply.github.com` 을 만들어 쓰고, API 가 막히면 `$OWNER@users.noreply.github.com` 으로 폴백한다(아이디 하드코딩 0 — 누가 받아도 그대로 작동). SETUP 7단계에 선제 설명 + `git log -1 --format='%an <%ae>' origin/main` 검증을 넣고, 막힐때표에 "GitHub엔 봇 커밋이 쌓이는데 웹 주소는 옛 데이터" 행을 추가. CLAUDE.md "자동화 켜줘" 항목에도 한 줄 명시 |
| B-056 | API장애 무증상+백필없음 | 이미 적용됨 | collect.js/discover.js 둘다 transcript=null이면 다음 실행에서 자동 재시도(백필)가 기본 흐름에 내장 |
| B-057 | DB컬럼없어 전량실패+초록불 | 해당없음 | DB/스키마 없음, writeJson은 필드 그대로 저장 |
| B-058① | 신규유입 2주 무증상(웹훅) | 해당없음 | 인바운드 웹훅/이벤트 자체 없음(전부 pull 구조) |
| B-059 | 설정안되면 통과(뒷문) | 해당없음 | 로그인/관리자 비번 없음. needKey()는 반대로 fail-closed(exit 0, 아무것도 안함) |
| B-060B | 하위호환 오탐기록 | 해당없음 | 대응하는 옵션생략 논쟁거리 없음(기록가치용 오탐사례) |
| B-058② | 삭제파일이 rewrite로만참조 | 해당없음 | vercel.json에 rewrites 없음(헤더만), 썸네일은 프록시 없이 data/thumbs 정적서빙 |
| B-061B | 정합성검사 1000건절단 | 해당없음 | DB 없음. fs.readFileSync가 파일 전체를 한번에 읽어 row-limit 절단 구조 자체 없음 |
| B-062B | 취소경로별 환불액 이원화 | 해당없음 | 예약/취소/환불 기능 없음 |
| B-097 | 성공토스트뿐인 가짜버튼 | 해당없음 | app.js 전량 재검토, fetch없는 성공알림 버튼 없음. 유일한 성공피드백(카드번호 복사)은 실제 navigator.clipboard.writeText 동작 |
| B-131 | 신규만타는입구 동기화누락 | 해당없음 | 입구가 문서2종으로 나뉜 구조 아님(ScrapeCreators 단일API), collect/discover는 대상 자체가 다름 |
| B-132 | 위저드작업 브라우저에만존재 | 해당없음 | 사용자 입력폼/위저드 없음(읽기전용) |
| B-133 | 서버리스죽어 analyzing좀비 | 해당없음 | 서버리스 요청/응답분리 없음(배치프로세스). 'analyzing' 같은 중간영속상태 자체가 없음(collected→analyzed 직행, 실패시 카운터만 증가, 다음실행 자동재시도) |
| B-134 | 200skip을 success오인 | 해당없음 | "완료도장 한번찍히면 안지워지는" 외부연동(Airtable류) 자체 없음 |
| B-135 | 403삼켜져 빈화면 | 이미 적용됨 | B-209와 동일지점. boot()가 `!settings||!posts||!posts.my` 명시적 falsy체크로 안내문구 표시(빈화면 방치 없음). 단 인증개념 자체가 없어 403케이스 발생불가 |
| B-136 | 위저드5단계 백업누락 | 해당없음 | 위저드 없음 |
| B-137 | 어림짐작경계로 데이터삭제 | 해당없음 | Notion연동/영역경계삭제 로직 없음 |
| B-138 | 모듈최상단client생성 전체사망 | 해당없음 | Gemini는 fetch를 함수내부에서만 호출(모듈스코프 클라이언트없음), 스크립트가 독립프로세스라 상호전파 불가 |
| B-139 | 페이지네이션 에러/끝 미구분 | 이미 적용됨 | lib.js scReels가 "1페이지실패=throw, 이후페이지실패=로그+break(부분결과유지)"로 명확히 구분. 캐시계층 자체 없음 |
| B-140 | 시트res.ok안보고파싱 | 해당없음 | 구글시트/CSV 소스 없음. 참고로 이 저장소 모든 fetch는 이미 res.ok 확인함 |
| B-141 | client팩토리미통일 조용히오분류 | 해당없음 | Anthropic류 SDK/우회설정 없음, Gemini 팩토리 2개뿐(geminiText/geminiAnalyze) |
| B-142 | 더보기커서 필터후개수전진 | 해당없음 | 더보기는 순수 클라이언트 슬라이싱, 서버측 AI필터개입 없음 |
| B-143 | 컬럼없는DB에서 본결과유실 | 해당없음 | DB 없음, writeJson은 객체전체 그대로 저장 |
| B-144 | 배치insert 한행실패매일전멸 | 해당없음 | DB batch insert·임베딩 없음 |
| B-145 | DB함수반환컬럼복사누락 | 해당없음 | DB 함수 없음 |
| B-146 | 자유텍스트enum CHECK없음 | 해당없음 | status/type/commerceHint 등은 단일원천 정규화함수 산출 enum. commerceOverrides 자유입력값도 알수없는값이면 기본배지로 받고 전체/기둥뷰엔 계속노출(완전실종 아님) |
| B-193 | 새규칙 N경로중하나빠뜨림 | 해당없음 | 자원생성 경로가 스크립트당 1곳뿐(다중진입점 구조 아님) |
| B-194 | 시간단위불일치(hours) | 해당없음 | 시간블록/예약단위 기능 없음 |
| B-195 | 제출신호만믿고성공표시 | 해당없음 | 폼제출/입금안내 플로우 없음 |
| B-196 | 오래된순+상한 신규사라짐 | 해당없음 | DB row-limit조회 없음. discover.js MAX_ITEMS=300은 오히려 반대로 최신순유지+오래된것부터버림(이미 올바른 설계) |
| B-197 | 크론이 조회실패삼키고0건초록불 | 이미 적용됨 | 전 스크립트 `main().catch(e=>{...;exit(1)})`로 미처리예외 반드시 빨간불. discover/analyze/coach.js는 "0건=성공아님" 명시판정 |
| B-209 | U.json null만반환 catch미작동 | 이미 적용됨 | 이 저장소 자체를 대상으로 이미 기록된 항목. util.js U.json(null계약)+app.js boot()의 명시적 falsy체크가 코드에 그대로 반영됨 |

**조치 필요 건수: 1건 → 수정됨(2026-09-01)**
- B-055 봇 커밋이 무료 Vercel(Hobby)에서 배포 안 될 수 있음 — daily/discover/weekly.yml의 커밋 작성자가 계정 소유자가 아니라서, 자동수집이 GitHub엔 쌓이는데 실제 배포된 사이트는 영구히 옛 데이터로 멈출 수 있음(GH Actions는 초록불이라 알아채기 어려움). SETUP.md가 증상은 문서화했지만 근본 원인 수정은 아직 없음.

부가 확인: daily(22:00 UTC 매일)와 discover(22:00 UTC 3일마다)는 3일에 한 번 같은 시각에 겹치지만, 세 워크플로 전부 `concurrency: group: data-write, cancel-in-progress: false`로 GitHub Actions 레벨에서 직렬화됨 — 동시성 문제 구조적으로 차단.

### 07·08 — 화면이 거짓말할 때 / 한 데이터 두 장부

| B번호 | 제목(축약) | 판정 | 근거/조치 |
|---|---|---|---|
| B-060 | 환불금액 화면 표시 | 해당없음 | 결제·환불 기능 자체 없음(정적 읽기전용 대시보드) |
| B-061 | 오류메시지 영문코드 노출 | 이미 적용됨 | `js/app.js:151-173` boot() 렌더 실패 시 한국어 안내문 우선, 원문 에러는 opacity.7 보조텍스트로만 |
| B-062 | 스크린샷 결함 오판 | 해당없음 | 이 저장소엔 스크린샷 기반 QA 스크립트 자체가 없음 |
| B-063 | 성능원인 오판(서버-DB 리전) | 해당없음 | DB·서버 왕복 없음. 브라우저가 정적 JSON을 직접 fetch |
| B-064 | 폰 가로스크롤(grid min-width) | 이미 적용됨 | `index.html:329` 모바일 미디어쿼리에 `.card{min-width:0;max-width:100%}` |
| B-065 | 검사도구 자신이 오염원 | 해당없음 | 로그인 기능 자체 없음, 검색창 오염 시나리오 불가 |
| B-066 | fetch 캐시로 낡은 값 표시 | 이미 적용됨 | `js/util.js:43` `fetch(url,{cache:'no-store'})` 이미 명시 |
| B-067 | 재시도 로그인 라우터 고착 | 해당없음 | 로그인 기능 자체 없음 |
| B-068 | CSP가 폰트CDN 차단 | 해당없음 | 외부 폰트 CDN 미사용(시스템 폰트 스택), CSP도 없음 |
| B-094 | 설정구역 이름-내용 불일치 | 해당없음 | 설정탭 5개 섹션(채널정체성/기둥/소스계정/자동화상태/크레딧) 제목과 내용 일치 확인 |
| B-100 | 같은 할일 세 탭 중복 | 해당없음 | 읽기전용이라 "처리할 일" 자체가 없음 |
| B-101 | 체크박스 CSS 오염 | 해당없음 | checkbox/radio 요소·타입무관 input 전역룰 grep 결과 없음 |
| B-102 | 화면 안 보고 배포 | 해당없음 | 배포/QA 관행 이슈로 코드 판정 대상 아님 |
| B-147 | AI처리 타임아웃-화면오류 | 이미 적용됨 | `ANALYZE_MAX`/`COACH_MAX`(analyze.js:25, coach.js:25)로 처리량 상한 + `weekly.yml:21` timeout-minutes:60 명시 예산 |
| B-148 | 오류필드명 불일치 | 해당없음 | 서버 API 자체 없음(정적 fetch만) |
| B-149 | 파라미터 이원화로 다른값 저장 | 해당없음 | 슬롯/일정변경 기능 없음 |
| B-150 | 안전장치가 필터 우회 | 해당없음 | 답변봇/필터 기능 없음 |
| B-151 | 화면숫자 하드코딩 | 해당없음 | 카운트 전부 데이터에서 계산, 하드코딩 리터럴 grep 결과 없음 |
| B-152 | 캐시/실시간 경로 데이터형태 불일치 | 해당없음 | 캐시/실시간 이중 경로 없음 |
| B-153 | 외부장애를 빈 상태로 표시 | 수정됨(2026-09-01) | `js/app.js:157-158` disc/analysis.json fetch 실패 시 `disc\|\|{items:[]}` 로 조용히 빈 구조 대체 — 진짜 실패와 "발굴 0건"이 화면에서 구분 안 됨(settings/posts만 fatal 처리) → `S._loadFailed` 배열에 못 읽은 파일 이름을 담고, 상단 `bootNote`를 지우는 대신 "불러오지 못했어요 — 데이터가 없어서가 아니라 못 읽어서입니다"로 바꾼다. 발굴 그리드의 빈 상태 문구도 같은 기준으로 갈라 쓴다. 나머지 탭은 계속 살려둔다(실측: discoveries.json 404 상태에서 라이브러리 12장 정상 렌더) |
| B-154 | 실패토스트인데 이미 저장됨 | 해당없음 | 저장/제출 폼 자체 없음 |
| B-155 | "진행중" 문구인데 미작동 | 이미 적용됨 | `cycleRows()`(app.js:1027-1039)+자동화상태 카드가 실제 마지막실행일 기준 경고 표시, 과장 문구 없음 |
| B-156 | Repository not found=계정문제 | 해당없음 | gh 계정 이슈는 운영 절차 항목, 코드 판정 대상 아님 |
| B-183 | 지어낸 UI 패턴(상자안상자 등) | 해당없음 | 세그먼트 칩 UI 사용, 상자중첩·이중줄 필터 패턴 미확인 |
| B-184 | 보안테스트가 사고로 오인 | 해당없음 | 인박스/알림 합산 기능 없음 |
| B-185 | 옵션 숨김(잠금 아님) | 해당없음 | 예약/옵션 선택 플로우 없음 |
| B-187 | grid `1fr`이 전체를 밀어냄 | 이미 적용됨 | `index.html:42` `main{min-width:0}` 최상위 grid 자식에 이미 적용, 위험한 flex-overflow 행 없음 |
| B-189 | type=number 0접두 잔존 | 수정됨(2026-09-01) | `js/app.js:863` 발굴 조회수 기준 입력이 `type="number"`(grep 1건 확인). React 버그는 아니나 스크롤휠 우발변경 등 일반 위험은 남음 — 텍스트+inputMode="numeric" 전환 검토 → 텍스트+`inputmode="numeric"`로 전환하고 `normalizeNumInput()`(숫자만 남기고 0 접두 제거)을 입력 즉시·적용 시 두 번 태운다. 이 함수는 PURE-BLOCK 안이라 `app-pure.test.js`가 `"007"→"7"`·`"12만"→"12"` 등을 회귀 검증(브라우저 실측도 `"007만a"→"7"`) |
| B-191 | 태그전체선택자가 신규요소 숨김 | 해당없음 | `display:none` 태그전체선택 위험 규칙 grep 결과 없음 |
| B-200 | router.refresh인데 state 고착 | 해당없음 | React/useState 없음, 부분갱신 개념 자체 없음(전체 재로드 구조) |
| B-201 | 탭전환시 편집상태 소실 | 해당없음 | 탭전환은 CSS display 토글일 뿐 DOM 언마운트 없음, 저장대기 편집상태도 없음 |
| B-070 | 두 저장소에 흩어진 명단 | 해당없음 | M-###(posts.json)·R-###(discoveries.json)는 다른 데이터 도메인의 독립 번호체계. analysis.json은 shortcode로만 참조, discoveries 자체 분석은 파일 내부 인라인 |
| B-071 | API 두 개, 검증은 한쪽만 | 해당없음 | 사용자 쓰기 API 자체 없음(읽기전용). GH Actions는 `concurrency: data-write`로 동시쓰기도 이미 차단 |
| B-072 | 매일수집인데 명단은 주1회 | 해당없음 | 팔로워 추이는 계정 1개뿐이라 "그릴 선 명단"이 불필요. 자동화상태 카드 주기 표시는 실제와 일치 |
| B-098 | 절반환불이 매출0원 | 해당없음 | 결제·정산 없음 |
| B-099 | 저장이 옛값으로 되돌림 | 해당없음 | 화면에 저장 버튼 자체 없음(읽기전용) |
| B-157 | 한 벌만 고쳐 갈라진 로직 | 수정됨(2026-09-01) | 동일 비즈니스 로직이 `js/app.js`와 `scripts/*.js`에 이중 구현: ① COMMERCE_AD/SELL 정규식이 app.js:78-79 ↔ lib.js:134-135에 리터럴 중복(주석 동기화 안내만 있고 자동검증 없음) ② 기둥 우선순위(overrides>analysis.pillars)가 app.js `pillarOf()`와 coach.js `buildCtx`의 병합 로직에 각각 구현 — 현재는 일치하나 한쪽만 고치면 갈라질 수 있음. 공유 모듈화 또는 `scripts/test/lib.test.js`에 동기화 검증 테스트 추가 권장 → 공유 모듈화는 템플릿 원칙(빌드 없음·브라우저는 require 불가)에 막혀 못 한다. 대신 ① 네 곳(app.js 정규식·pillarOf, lib.js 정규식, coach.js buildCtx)에 서로를 가리키는 주석을 달고 ② `scripts/test/app-sync.test.js`를 새로 만들어 정규식 source·flags 글자 대조 + 같은 캡션 7종의 판정 일치 + 기둥 우선순위 순서 + 주석 존재까지 매 테스트마다 검증한다 |
| B-158 | 매직문자열 상태값 이모지 불일치 | 해당없음 | status 'collected'/'analyzed'는 이 저장소 유일한 writer/reader만 사용, 외부 이중표기 시스템 없음 |
| B-159 | DB함수 오버로드 충돌 | 해당없음 | DB 없음 |
| B-160 | 지식DB 사본이 원본 정책 안따름 | 해당없음 | RAG/지식DB 없음 |
| B-161 | 한 컬럼 두 의미체계 | 해당없음 | 필드 하나가 두 의미로 쓰이는 곳 확인 안 됨 |
| B-162 | 로컬vs서버 시각비교 없이 복원 | 해당없음 | 로컬스토리지는 표시설정(discThr)만, 서버데이터 되쓰기 없음 |
| B-163 | 만료URL을 캐시에 저장 | 해당없음 | 서명URL은 즉시사용후 폐기, 썸네일은 영구 로컬파일로 다운로드 |
| B-164 | 두 장부로 세면 횟수가 다름 | 해당없음 | 카운트/한도 기능 없음 |
| B-165 | 외부스키마 속성삭제 못 감지 | 해당없음 | 단일 정규화 지점 + null 폴백(정규식으로 확인), 이중소비 매칭 시나리오 없음 |
| B-166 | 스키마버전 없이 구조변경 | 해당없음 | 다중 리더가 서로 다른 구버전 처리하는 구조 없음 |
| B-167 | 안쓰는 쌍둥이 테이블 방치 | 해당없음 | DB 없음 |
| B-198 | 환불정책값vs실제값 혼용 | 해당없음 | 결제·환불 없음 |

**조치 필요 건수: 3건 → 3건 모두 수정됨(2026-09-01)**
- B-153 — disc/analysis.json 로드 실패가 "발굴 0건"과 동일한 화면으로 보임(에러 구분 안 됨)
- B-157 — 상업성 정규식·기둥 우선순위 로직이 app.js와 scripts/*.js에 이중 구현(현재는 일치, 자동 동기화 검증 없음)
- B-189 — 발굴 조회수 기준 입력이 `type="number"` 1건 남음(텍스트+inputMode 전환 검토)
### 09·10 — AI와 자동화가 배신할 때 / 복제와 이식

| B번호 | 제목(축약) | 판정 | 근거/조치 |
|---|---|---|---|
| B-080 | 반말/존댓말 지시 모호 | 이미 적용됨 | `scripts/analyze.js`61행·`scripts/coach.js`80행 — "~했습니다"/"~하는 것이 중요합니다" 등 종결어미 좋은/나쁜 예를 명시, 반말 금지 명문화 |
| B-081 | 프롬프트 변경 후 소급 | 수정됨(2026-09-01) | `analyze.js`/`coach.js`가 `status='analyzed'`/`analysis.coaching[sc]`를 영구 기록만 함. 프롬프트를 고쳐도 기존 산출물을 재분석 큐로 되돌리는 절차·플래그가 없음 → 두 스크립트에 `PROMPT_VERSION` 상수를 두고 산출물에 같이 적는다(`item.promptVersion` / `coaching[sc].prompt_version`). 이제 "옛 버전으로 만든 것만" 고를 수 있다. 재분석 실행 자체는 이번 범위 밖 — 되돌리는 법(analyze: `status`를 `'collected'`로 / coach: `coaching`에서 그 shortcode 삭제)만 주석에 남겼다 |
| B-082 | 무인배치 전면실패 정지 | 해당없음 | `ANALYZE_MAX`(10)·`COACH_MAX`(5)로 상한 걸린 1회성 크론 실행이라 "밤새 헛도는 루프" 구조 자체가 없음. `discover.js`는 오히려 `isKeyOrCreditError()`로 키/크레딧 오류시 조기 중단(74-78행) |
| B-083 | pgrep 자기매칭 | 해당없음 | pgrep 기반 대기/체인 셸 스크립트 없음 |
| B-084 | 검증실패시 재시도·타임스탬프 | 수정됨(2026-09-01) | `lib.js` `extractJson`(334-337행)이 JSON 파싱 실패시 예외 없이 `{raw:...}`를 반환 → `geminiText`가 재시도하지 않고 그대로 통과 → `analyze.js`/`coach.js`가 이 쓰레기값을 `status='analyzed'`로 영구 확정 → `extractJson`이 실패 시 `parseFailed:true` + `parseFailedAt`(ISO 타임스탬프)를 함께 담는다. `geminiText`는 이 표시를 보고 최대 3회까지 다시 물어보고, 끝내 실패하면 표시가 남은 채로 돌려준다(파이프라인은 안 막되 흔적은 남는다). `geminiAnalyze`도 같은 기준으로 1회 재시도. `lib.test.js`에 회귀 테스트 추가 |
| B-085a | 감시자가 먼저 죽음 | 해당없음 | grep -c 기반 셸 감시 스크립트 없음 |
| B-085b | 배포훅 cwd 오인 | 해당없음 | 이 저장소엔 배포 알림 훅이 없음(`.claude/`엔 CLAUDE.md 하나뿐) |
| B-086 | 정규식으로 HTML 수술 | 해당없음 | 마크업을 정규식 치환으로 고치는 파이프라인 없음(수작업 편집만) |
| B-087 | 실패항목 반복과금 | 이미 적용됨 | `analyze.js`117행·`coach.js`143행 — `analyzeFails`/`coachFails` 카운터를 JSON에 영속화, `MAX_FAILS=3` 도달시 선정 제외 |
| B-088 | 돈나가는 버튼 연타 | 해당없음 | 프론트(`js/app.js`)는 완전 읽기전용, AI 호출 트리거 버튼 자체가 없음(전부 CLI/GitHub Actions 배치) |
| B-095 | SSR 주석 문자열 검색 오탐 | 해당없음 | 서버 렌더링·문자열검색 기반 검증 로직 없음(순수 클라이언트 렌더) |
| B-096 | 비활성버튼 오탐 테스트 | 해당없음 | Playwright 등 자동 UI 테스트 없음(`lib.test.js`는 순수함수만 테스트) |
| B-168 | thinking에 응답파트 밀림 | 수정됨(2026-09-01) | `lib.js` `geminiText`/`geminiAnalyze`가 `generationConfig.thinkingConfig` 없이 `parts?.[0]?.text` 고정 인덱스로 읽음. 모델이 thought 파트를 앞에 얹으면 빈 텍스트를 받을 위험 → `candidateText()`를 새로 만들어 `thought:true` 파트를 걸러내고 남은 `text`를 전부 이어 붙인다(고정 인덱스 제거). 두 함수 모두 이걸 쓰고, `lib.test.js`가 "생각 파트가 앞에 와도 본문이 나온다"를 검증 |
| B-169 | 없는 이슈 지어내기(오디오) | 해당없음 | 오디오/나레이션 판정 기능 자체가 없음 |
| B-170 | 코치 vs AI 대조 누락 | 해당없음 | 사람 코치 피드백과 AI 산출물을 정기 대조하는 루프/기능이 없음 |
| B-171 | 프록시가 시스템프롬프트 주입 | 해당없음 | 프록시/게이트웨이 없이 `generativelanguage.googleapis.com`에 자체 키로 직접 호출 |
| B-172 | max_tokens 절단 | 수정됨(2026-09-01) | `geminiText` 호출에 `maxOutputTokens` 명시 없고 `finishReason` 검사도 없음. 항목 상한(3개·2개)이 작아 위험은 낮지만 잘려도 감지 불가(B-084와 같은 구멍) → `generationConfig.maxOutputTokens`를 4096으로 명시(`GEMINI_MAX_OUTPUT_TOKENS`로 조절), 공통 `checkFinish()`가 `finishReason !== 'STOP'`이면 경고를 찍고 `MAX_TOKENS`면 "예산을 늘리세요"까지 안내한다. `geminiText`·`geminiAnalyze` 두 경로 모두 적용 |
| B-173 | AI가 순번을 id로 착각 | 해당없음 | AI가 후보 순번/ID를 골라 반환하는 구조 자체가 없음(`classify.js`는 이미 `scSet.has(sc)`로 존재 검증) |
| B-174 | 접두어 지어낸 태그 실종 | 이미 적용됨 | `classify.js`55-59행 — `valid = new Set([...pillars,'미분류'])`로 어휘 밖 값은 폐기 |
| B-175 | 저품질 결과 캐시 박제 | 해당없음 | TTL/캐시 시스템 자체가 없음(파일은 매 실행 통째로 재작성) |
| B-176 | 다단계 파이프라인 변동성 | 해당없음 | 7-8단계 RAG 파이프라인 없음. 항목당 Gemini 호출 1~2회(시각분석+종합) |
| B-177 | 텍스트를 조인키로 사용 | 해당없음 | 병합 로직이 전부 `shortcode` 고정키 사용, AI 재작성 텍스트를 키로 쓰는 곳 없음 |
| B-178 | thinking이 출력예산 잠식 | 수정됨(2026-09-01) | B-168과 동일 원인 — thinking 예산 분리 설정과 `finishReason` 검사가 전 호출부(`geminiText`/`geminiAnalyze`)에 없음 → `generationConfig.thinkingConfig.thinkingBudget`(기본 1024, `GEMINI_THINKING_BUDGET`으로 조절)을 붙여 생각 예산과 출력 예산을 분리한다. 모델이 `thinkingConfig`를 모르면 400 응답에서 감지해 그 호출만 한 번 다시 보내고 이후로는 아예 안 붙인다(`thinkingSupported` 플래그) — 모델을 바꿔도 코드를 안 고쳐도 되게. `finishReason` 검사는 B-172와 같은 공통 경로 |
| B-179 | 검색결과 최신성 가중치 없음 | 수정됨(2026-09-01) | `js/app.js`913행 발굴 그리드가 순수 조회수(`views`) 정렬. 최신성 가중치 없어 오래된 대박 릴스가 계속 상단 점유 가능(단, 300건 상한·계정당 연속노출 제한으로 크미나이 사례보다 영향 작음) → 레퍼런스 탭에 정렬 렌즈 2개(`조회순` 기본 유지 / `최신 가중`)를 추가. 최신 가중은 `recencyScore = 조회수 × 0.5^(경과일/45)`(반감기 45일)이고 날짜를 모르면 맨 뒤로 보낸다. 계정 도배 방지 재배열은 두 렌즈에 그대로 적용. 저장 버튼은 여전히 없다 — 렌즈 선택은 이 브라우저 세션 안에서만 산다 |
| B-186 | 원본 안읽고 YAML 수술 | 이미 적용됨 | 워크플로 3개(`daily.yml`·`discover.yml`·`weekly.yml`) `yaml.safe_load`로 파싱 확인 — `on:` 블록 중복·밀림 없음 |
| B-210 | node --test 글롭 미지정 | 이미 적용됨 | `package.json`7행 `"test": "node --test \"scripts/test/**/*.test.js\""` — 이 버그가 바로 이 저장소에서 발견·수정된 건 |
| B-090 | 이식본 썸네일 주소 하드코딩 | 이미 적용됨 | `lib.js`19행 `THUMB_WEB='data/thumbs'` 상대경로, `vercel.json`엔 rewrite 자체가 없음 |
| B-091 | 새 DB에서 ALTER 순서 사망 | 해당없음 | DB/SQL 없음(JSON 파일 저장) |
| B-092 | 이모지 반쪽 절단 | 이미 적용됨 | `lib.js`139-143행 `cutSafe()` 서로게이트 경계 처리 + `lib.test.js`101-105행 단위테스트 |
| B-093 | 서버리스에서 fs 프롬프트 못읽음 | 해당없음 | 프롬프트가 이미 인라인 JS 상수(`VISUAL_PROMPT`)이고, Vercel 배포가 순수 정적(서버리스 함수 자체가 없음 — `vercel.json`은 헤더만) |
| B-180 | env 따옴표째 등록 | 이미 적용됨 | `lib.js`28행 `parseEnv`가 `.replace(/^["']|["']$/g,'')`로 따옴표 제거. Vercel엔 서버리스 함수가 없어 `vercel env add` 경로 자체가 존재하지 않음 |
| B-181 | 빈 DB 마이그레이션 재현 실패 | 해당없음 | DB/마이그레이션 없음 |
| B-182 | 복제 라우트 캐시키 불일치 | 해당없음 | 캐시·중복 API 라우트 없음 |
| B-207 | 배포·DB 스키마 시점 어긋남 | 해당없음 | DB 컬럼 개념 없음. JSON은 스크립트 실행마다 그 버전 그대로 통째로 다시 씀 + `app.js`가 기본값/옵셔널체이닝으로 누락 필드 방어 |
| B-211 | 더미데이터가 품질게이트 기준선 | 이미 적용됨 | `lib.js` `isDemo`/`freshIfDemo`/`clearDemoThumbs`(76-97행) — 이 버그가 바로 이 저장소에서 발견·수정된 건 |
| B-214 | 키없음 가드 순서 잘못 | 이미 적용됨 | `discover.js`42-46행 포함 전 스크립트(`analyze.js`·`coach.js`·`classify.js`·`brief.js`·`snapshot.js`)가 `isDemo` 체크를 `needKey`보다 먼저 수행 |

조치 필요 건수: 6건 → 6건 모두 수정됨(2026-09-01)

- B-081 프롬프트 변경 시 기존 analyzed/coaching 산출물을 소급 재생성하는 절차 없음
- B-084 Gemini JSON 파싱 실패시 예외 없이 `{raw:...}`로 조용히 통과해 재시도 없이 완료 처리됨
- B-168 Gemini 호출에 thinking 비활성화 설정 없이 `parts[0].text` 고정 인덱스로 읽어 응답 파싱 실패 위험
- B-172 `maxOutputTokens` 미설정·`finishReason` 미검사로 응답 절단 감지 불가
- B-178 (B-168과 동일 원인) thinking 예산과 출력 예산을 분리 관리하지 않음, 전 호출부 공통
- B-179 발굴 그리드 정렬에 최신성 가중치가 없어 오래된 대박 릴스가 계속 상단을 점유할 수 있음
### 종합 — 조치 필요 16건 → 2026-09-01 전건 수정 완료

| B번호 | 갈래 | 무엇 |
|---|---|---|
| B-016 | 01 | 소스 계정·내 핸들 등록 시 실제 인스타 프로필 존재·동일인 여부 검증 없음(오타 핸들이 실존 타인 계정을 가리키면 조용히 섞임) |
| B-188 | 01 | 발굴 임계값 `type=number` 입력창에 휠 가드 없음 |
| B-116 | 04 | `js/app.js:270` `new Date().toISOString().slice(0,7)`이 브라우저 로컬시각을 UTC로 강제 변환 — 매월 1일 00~09시 KST에 '이번 달' 라벨 오표시 |
| B-122 | 04 | `discover.js` 보관 상한 정리 시 썸네일 파일을 먼저 지우고 JSON을 나중에 씀 — 중간에 죽으면 JSON엔 남았는데 파일은 없는 상태 가능 |
| B-124 | 04 | `geminiAnalyze()`에 `geminiText()`와 같은 빈 응답/finishReason 검증이 빠짐 |
| B-126 | 04 | `downloadThumb()`이 Content-Type/최소 바이트 검증 없이 저장 — CDN이 로그인월 HTML을 200으로 주면 .jpg로 오저장 가능 |
| B-055 | 06 | 자동화 커밋(daily/discover/weekly.yml)이 봇 계정으로 커밋 — 무료 Vercel(Hobby)에서 배포 안 될 수 있고, 이 저장소는 DB가 아니라 정적 JSON 자체가 데이터라 사이트가 영구히 구데이터로 멈출 위험 |
| B-153 | 07 | disc/analysis.json 로드 실패가 '발굴 0건'과 동일한 화면으로 보여 진짜 실패와 구분 안 됨(settings/posts만 치명 처리) |
| B-157 | 07 | 상업성 정규식·기둥 우선순위 로직이 `js/app.js`와 `scripts/*.js`에 이중 구현(현재는 일치하나 자동 동기화 검증 없음) |
| B-189 | 07 | 발굴 조회수 기준 입력이 `type=number` 1건 남음 |
| B-081 | 09 | 프롬프트를 고쳐도 기존 analyzed/coaching 산출물을 소급 재생성하는 절차 없음 |
| B-084 | 09 | Gemini JSON 파싱 실패 시 예외 없이 `{raw:...}`로 조용히 통과해 재시도 없이 `analyzed` 확정 |
| B-168 | 09 | Gemini 호출에 thinking 비활성화 설정 없이 `parts[0].text` 고정 인덱스로 읽어 응답 파싱 실패 위험 |
| B-172 | 09 | `maxOutputTokens` 미설정·`finishReason` 미검사로 응답 절단을 감지 못함 |
| B-178 | 09 | (B-168과 동일 원인) thinking 예산과 출력 예산 분리 관리 안 함, 전 Gemini 호출부 공통 |
| B-179 | 09 | 발굴 그리드 정렬에 최신성 가중치가 없어 오래된 대박 릴스가 계속 상단 점유 가능 |

그 외 항목은 '이미 적용됨'(코드에 이미 그 교훈이 반영, 특히 B-208·B-209·B-210·B-211·B-212·B-213·B-214는 이 저장소 자체가 원 사례) 또는 '해당없음'(로그인·DB·결제·문자발송·예약 등 이 정적 대시보드에 없는 상황)이다.

**2026-09-01 — 위 16건 전부 수정 완료.** 항목별로 "어떻게 고쳤는지"는 갈래별 표의 근거 칸 끝에 붙여 두었다.
검증: `npm test` 21건 통과(기존 12건 + 신규 9건 — `app-pure.test.js`·`app-sync.test.js`·extractJson·candidateText), `node --check` 전 파일 통과, 워크플로 3개 YAML 파싱 통과, `serve.js`로 index.html·JSON 4개 200, 키 없이 `collect.js` 실행 시 notice + exit 0 유지, 세 탭 브라우저 콘솔 오류 0.
템플릿 원칙은 그대로다 — 서버·DB·저장 버튼·외부 라이브러리·`package.json` 의존성 추가 없음(테스트는 Node 내장 러너).
